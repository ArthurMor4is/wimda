
version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model with customer data"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model with other data but don't customer data"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
